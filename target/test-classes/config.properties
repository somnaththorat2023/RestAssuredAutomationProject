#test properties
environment=dev
qa_browser=chrome
qa_headless=false
# note, proxy host should have no scheme, just the domain, and usually works best with VPN on
#qaProxy=true
#qaProxyHost=
#qaProxyPort=8080
#qaNoProxy=::1,127.0.0.1,localhost

#Logging Properties
webdriver_log_enabled=false
